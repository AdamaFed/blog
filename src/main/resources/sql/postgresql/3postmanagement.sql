CREATE TABLE post (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    post_title VARCHAR(50) NOT NULL,
    post_content VARCHAR(2000) NOT NULL,
    written_at TIMESTAMP NOT NULL,
    user_id INTEGER
);



ALTER TABLE post
ADD CONSTRAINT fk_userin_post
FOREIGN KEY (user_id)
REFERENCES userin(id);